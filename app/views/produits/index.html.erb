<p id="notice"><%= notice %></p>

<h1>Produits</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Prix</th>
      <th>Description</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @produits.each do |produit| %>
      <tr>
        <td><%= produit.name %></td>
        <td><%= produit.prix %></td>
        <td><%= produit.description %></td>
        <td><img src=<%= produit.image %> width="100" height="100"/></td>
        <td><%= link_to 'Show', produit %></td>
        <td><%= link_to 'Edit', edit_produit_path(produit) %></td>
        <td><%= link_to 'Destroy', produit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Produit', new_produit_path %>
